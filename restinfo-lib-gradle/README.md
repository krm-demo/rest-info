# rest-info-lib-gradle
Source code of `rest-info-lib` that are built by `gradle` build-tool                    
and deployed to remote artifactory `...github...` as `org.krmdemo.restinfo:rest-info-lib-gradle`.

